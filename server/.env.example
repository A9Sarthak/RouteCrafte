# Server port
PORT=8080

# JWT secret used to sign tokens (change this to a strong value in production)
JWT_SECRET=change-me-to-a-secure-random-string

# MongoDB Connection String (fallback is mongodb://127.0.0.1:27017/routecraft)
MONGO_URI=mongodb://127.0.0.1:27017/routecraft
